# GPU Status 2026-06-02 20:25 +0800

Current `nvidia-smi` showed no spare full GPU.

Active `zwl` runs:

- GPU 0: S0 DOTA2 RoI Transformer valid-PNG restart, PID `1077281`, screen `1077110.s0_dota2_roi_trans_rebuild_validpng_20260602_gpu0`, latest log `Epoch(train) [3][35650/39007]`, ETA about `20:11:37`.
- GPU 6: GeoNexus S3 scene-adapter 144e, PID `425496`, screen `425331.geonexus_s3_scene_adapter_144e`, latest log `Epoch(train) [89][1400/1410]`, ETA about `10:27:21`.

Recently finished or failed `zwl` runs:

- S2 hierarchy regularizer 144e completed validation at `2026-06-02 16:25:54`; final epoch 144 `dota/mAP=0.3723`, `dota/AP50=0.3720`; best observed epoch 30 `dota/mAP=0.3819`, `dota/AP50=0.3820`.
- S0 DOTA2 Oriented R-CNN R50 valid-PNG failed after entering training at `Epoch(train) [1][300/39007]` with CUDA out-of-memory. The log reports local `GPU 0`, which corresponds to the process-visible CUDA device, not necessarily physical GPU 0.

Other users' active GPU jobs:

- GPU 1 and GPU 2: `lyc` cmnext two-process training, PIDs `2029712` and `2029713`.
- GPU 3: `lyc` `gate_eva_gta_0602`, PID `1965722`.
- GPU 4: `lyc` `gate_eva_c2bm_0525`, PID `2559760`.
- GPU 5: `lyc` ACC SwinT pretrain, PID `2500095`.

Decision:

- Do not launch the next experiment now. S2 freed GPU 2, but another user's job now occupies it, and all physical GPUs 0-6 have active compute processes.
- Next useful action when a GPU frees: either continue the planned GeoNexus sequence after S3 144e finishes, or relaunch the ORCNN DOTA2 baseline with lower memory pressure.
