# Agent Entry Point

Use `New/` as the project memory root.

Before making research, code, documentation, or experiment-workflow changes,
read `PROJECT_INSTRUCTIONS.md` selectively: start with the top operational
sections, especially the token-saving startup protocol and command/GPU failure
playbook.

Avoid loading the full historical sections unless the task needs route evidence,
experiment provenance, or paper-claim support.
