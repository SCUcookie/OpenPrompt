New requirement
As the research has been several months and the openprompt paper published about one year(which means too far away from now), as we all know that if we want to publish any papers we need to use the new method(it will be best to use the newest trend for the past 1 month's top papers' as baseline).
So you need to find the 2026- new published papers to see if any method(must containing code) as baseline, involving llms or other hot methods will be better.
It'll be better to use Remote Sensing Object Detection(or any fields related to object detection or semantic segmentation).
Also, openprompt is a idea method but the time is too long from now, while it requires a lot of resources(500g+ files) to reproduce.
Any approach involving my knowledge will be easy for me to complete:
1.Gumbel softmax
2.llm
3.compression(knowledge distillation、quantization、pruning)
